# Homework Two

Static site for Homework Two.

## Getting started

- Install dependencies: `npm install`
- Run the Sass watcher: `npm run compile:sass`
- Start the dev server: `npm run serve`

## Structure

- `index.html` main page
- `scss/` source styles
- `css/` compiled styles
- `images/` assets
